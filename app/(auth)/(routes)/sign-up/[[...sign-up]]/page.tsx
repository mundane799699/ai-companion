import {
  ClerkProvider,
  SignInButton,
  SignUp,
  SignUpButton,
  SignedIn,
  SignedOut,
  UserButton,
} from "@clerk/nextjs";

const SignUpPage = () => {
  return <SignUp />;
};

export default SignUpPage;
